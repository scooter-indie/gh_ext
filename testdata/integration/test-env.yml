# Integration test environment configuration
# DO NOT USE FOR REAL WORK - This project is for automated testing only

github:
  # Test repository
  repo: scooter-indie/gh-pm-test

  # Test project
  project:
    owner: scooter-indie
    number: 13
    id: PVT_kwHOAATLPs4BJq7a
    url: https://github.com/users/scooter-indie/projects/13

  # Test issues (created for integration tests)
  issues:
    - number: 1
      title: "Test Issue 1"
    - number: 2
      title: "Test Issue 2 - Parent"
    - number: 3
      title: "Test Issue 3 - Sub-issue"

  # Custom fields added to test project
  fields:
    - name: Priority
      type: SINGLE_SELECT
      options: [P0, P1, P2]
    - name: Size
      type: SINGLE_SELECT
      options: [XS, S, M, L, XL]

# Instructions for running integration tests:
# 1. Ensure GH_TOKEN is set with appropriate permissions
# 2. Run: go test -tags=integration ./...
